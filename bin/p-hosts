#!/usr/bin/env node

var program = require('commander');

program
	.version('0.0.1');

program
	.command('list', 'list all hosts')
	.command('add', 'add a new host');

program.parse(process.argv);

if (!program.runningCommand) {
	program.help();
}